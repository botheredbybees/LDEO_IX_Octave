function h = colorbar(varargin)
% octave_harness stub: no-op plotting (CONTINUATION_PLAN.md M2). Real
% colorbar() crashed here ("PROP/VAL inputs must occur in pairs") on a
% MATLAB-style call Octave's colorbar() doesn't accept.
  if nargout > 0
    h = 1;
  end
end
