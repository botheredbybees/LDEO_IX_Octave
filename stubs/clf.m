function clf(varargin)
% octave_harness stub: no-op plotting (CONTINUATION_PLAN.md M2). Needed
% because figure() is stubbed to a dummy handle that doesn't correspond to
% a real graphics object -- real clf()'s get() on that handle errors.
end
